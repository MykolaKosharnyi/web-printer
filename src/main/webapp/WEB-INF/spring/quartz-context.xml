<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">
 
    <!-- For times when you just need to invoke a method on a specific object -->
    <beans:bean id="simpleJobDetail" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <beans:property name="targetObject" ref="loaderValueCurrency" />
        <beans:property name="targetMethod" value="loadCurrency" />
    </beans:bean>
 
    <!-- Run the job every 2 hours with initial delay of 1 minute -->
    <beans:bean id="simpleTrigger"  class="org.springframework.scheduling.quartz.SimpleTriggerFactoryBean">
        <beans:property name="jobDetail" ref="simpleJobDetail" />
        <beans:property name="startDelay" value="#{1000*60}" />
        <beans:property name="repeatInterval" value="#{1000*60*60*2}" />
    </beans:bean>
 
    <!-- Scheduler factory bean to glue together jobDetails and triggers to Configure Quartz Scheduler -->
    <beans:bean  class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        <beans:property name="jobDetails">
            <beans:list>
                <beans:ref bean="simpleJobDetail" />
            </beans:list>
        </beans:property>
 
        <beans:property name="triggers">
            <beans:list>
                <beans:ref bean="simpleTrigger" />
            </beans:list>
        </beans:property>
    </beans:bean>
 
</beans:beans>